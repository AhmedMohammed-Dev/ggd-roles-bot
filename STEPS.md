# ✅ الخطوات المتبقية — بالترتيب وبأبسط شكل

> **الخلاصة:** كل شيء جاهز ومُختبَر. المتبقّي عليك **خطوة واحدة حرجة: لصق التوكن**.
> وبعدها للنشر 24/7 عندك طريقان: **أمر واحد** (القسم ⚡ — الأسهل) أو الخطوات اليدوية (الخطوة 4).
> الوقت المتوقّع: 5–10 دقائق.

---

## 📦 ما تم إنجازه بالفعل (لا تحتاج فعل أي شيء فيه)

| ✅ | البند |
|---|---|
| ✅ | كود البوت كامل بـ **86 دوراً** (كل أدوار اللعبة) + أزرار + صفحات + **صور** (روابط حقيقية مفحوصة من ويكي اللعبة) |
| ✅ | كل ضغطة تُرسل بطاقة الدور **لك وحدك** (ephemeral) |
| ✅ | ملف `.env` **جاهز ومنتظر التوكن فقط** (وهو مستثنى من الرفع على GitHub) |
| ✅ | اختبارات تنجح كلها + ملف فحص جاهزية `check_setup.py` + ضبط تلقائي `setup_discord.py` |
| ✅ | ملف تشغيل بضغطة واحدة `START.bat` (ويندوز) و `start.sh` (لينكس/ماك) |
| ✅ | ملف `push.bat` يرفع الكود على GitHub بنفسه (باسم مستخدمك) |
| ✅ | إعداد Render (`render.yaml`) و Dockerfile للحاويات |
| ✅ | مستودع Git محلي جاهز مع حفظات (Commits) — ينقصه الرفع فقط |
| ✅ | الكود مرفوع فعلاً على مستودعك `AhmedMohammed-Dev/ggd-roles-bot` (‏18 ملفاً) |
| ✅ | سكربت **`deploy.py`** ينشر كل شيء بأمر واحد: مستودع GitHub + رفعه + خدمة Render + كل الإعدادات |
| ✅ | 87 اختباراً تنجح كلها (45 للبوت + 42 للنشر)، ومنها اختبار يمنع رفع أي ملف سرّي، واختبار يفشل لو نقص دور واحد من الدليل |
| ✅ | سكربت `check_images.py` يفحص روابط الصور كلها على الإنترنت (كلها تعمل حالياً) |

---

## ⚡ الطريق السريع — أمر واحد ينفّذ كل شيء

> هذا البديل الأسهل للخطوة 4 بالكامل: سكربت واحد ينشئ مستودع GitHub، ويرفع الكود،
> وينشئ الخدمة على Render، ويضبط كل المتغيّرات، وينتظر حتى تعمل. **لا تنسخ ولا تلصق أي إعداد.**

### ✅ ما تم فعلاً (لا تحتاج عمل أي شيء فيه)

| ✅ | البند |
|---|---|
| ✅ | تسجيل الدخول إلى GitHub تم في المتصفح (الحساب: `AhmedMohammed-Dev`) — بلا أي مفتاح ولا ملف |
| ✅ | المستودع `ggd-roles-bot` صار محدّثاً: **18 ملفاً** بأحدث نسخة، وتاريخك القديم محفوظ |
| ✅ | فحص أمان للمحتوى: لا توكن ولا مفتاح داخل أي ملف على GitHub |
| ✅ | المفتاح المستخدم لا يُطبع ولا يُحفظ في أي ملف (يبقى في مخزن Windows الآمن) |

### ⬅️ المتبقّي عليك: نقرة واحدة على Render

افتح هذا الرابط (سجّل الدخول بحساب GitHub نفسه):

```
https://render.com/deploy?repo=https://github.com/AhmedMohammed-Dev/ggd-roles-bot
```

ثم: الصق `DISCORD_TOKEN` في الحقل الوحيد → **Apply** → انتظر 2–4 دقائق → اكتب `/roles`. 🎉

> **ملاحظة:** المستودع خاص (Private) حالياً. الزر يعمل فوراً إن جعلته **عاماً**
> (Settings → Change visibility)؛ وإن أردته خاصاً فأضف لـ Render صلاحية الوصول لمستودعك
> مرة واحدة (الرابط نفسه سيعرض عليك الخطوة) — وبعدها يعمل الزر عادياً.

---

## 🔑 أو: الطريق اليدوي بالكامل (مفتاحان + أمر واحد)

المطلوب منك **مرة واحدة**: مفتاحان تنسخهما بضغطة زر، ثم أمر واحد.

### A) مفتاح GitHub

1. افتح <https://github.com/settings/personal-access-tokens/new>
2. الاسم: `ggd-deploy` — المدة: `7 days`
3. **Repository access** → اختر **All repositories**
4. انزل لقسم **Permissions** → **Repository permissions** واضبط بندين:
   - `Administration` → **Read and write**  ← مطلوب لإنشاء المستودع
   - `Contents` → **Read and write**  ← مطلوب لرفع الكود
5. اضغط **Generate token** ثم **Copy**.

> 💡 هذا **ليس** توكن البوت: هو مفتاح يسمح للبرنامج فقط بإنشاء مستودعك ورفع الكود،
> وتقدر تلغيه (Revoke) من نفس الصفحة في أي وقت — الأفضل بعد ما يخلص النشر.
> ولو مفتاحك لا يملك صلاحية الإنشاء: اعمل المستودع يدوياً من <https://github.com/new>
> (اسمه `ggd-roles-bot`، فارغاً بلا README) ثم أعد الأمر وسيكمل من مكانه.

### B) مفتاح Render

1. افتح <https://dashboard.render.com/u/settings#api-keys>
2. **Create API Key** → الاسم `ggd-deploy` → **Copy**.
   (إن طلب Render تأكيد الحساب أولاً فسجّل بالبريد أو Google — ثانيتان.)

### C) ضعهما في الملف

انسخ الملف **`.deploy_secrets.example`** باسم **`.deploy_secrets`** (بالضبط، بلا إضافات)
وافتحه والصق المفتاحين بعد علامتي المساواة.

> 🔒 الملف مذكور في `.gitignore` فلا يُرفع إلى GitHub أبداً، ولا يوجد أي مفتاح داخل الكود.

### D) أمر واحد

```bash
python deploy.py --check     # (مستحسن) يتأكد أن المفتاحين يعملان — لا يغيّر شيئاً
python deploy.py             # ينشر كل شيء
```

يطبع لك في النهاية رابط التطبيق ولوحة التحكم، ويخبرك متى تكتب `/roles` في ديسكورد.

> **لو كان المستودع خاصاً (Private)** يحتاج Render إشارة واحدة منك أول مرة:
> <https://dashboard.render.com> → **New + → Web Service** → اربط GitHub واختر المستودع
> (مش لازم تكمّل الإنشاء — الربط وحده يكفي) → ثم أعد `python deploy.py` فيكمل الباقي وحده.
> **أو** اجعل `REPO_VISIBILITY=public` في ملف المفاتيح فيصير كل شيء تلقائياً بلا أي خطوة في المتصفح.

---

## 🔑 الخطوة 1 — أعطني التوكن (هذه هي الخطوة الوحيدة المطلوبة منك)

### 1.1 أنشئ تطبيقك على ديسكورد

1. افتح هذا الرابط: **<https://discord.com/developers/applications>**
2. اضغط الزر الأزرق **New Application** (أعلى اليمين).
3. اكتب أي اسم، مثلاً: `GGD Roles` → اضغط **Create**.

### 1.2 خُذ التوكن

1. من القائمة على اليسار اضغط **Bot**.
2. اضغط زر **Reset Token** → ثم **Yes, do it!**
   - ⚠️ إن ظهرت لك كلمة المرور أو رمز 2FA فاكتبه، هذا طبيعي.
3. اضغط **Copy** (أو **Copy Token**).
   - 💡 التوكن نصّ طويل يحوي **نقطتين**، مثل: `MTA1...xYz.abc123.XyZ...789`
   - 🔒 **هذا النصّ هو مفتاح البوت بالكامل.** لا ترسله لأي شخص ولا لأي موقع ولا لي.
     من يملكه يملك البوت — ولو تسرّب فأعد Reset Token فيتوقف القديم فوراً.

### 1.3 الصقه في الملف

1. افتح مجلد `ggd-roles-bot`.
2. افتح الملف المسمّى **`.env`** بأي محرّر نصوص (Notepad أو VS Code).
   - 💡 إن لم تره: الملف يبدأ بنقطة، في **Notepad** اكتب في خانة الاسم `".env"` مع علامتي التنصيص،
     أو من داخل المجلد شغّل `cmd` واكتب: `notepad .env`
3. ستجد هذا السطر:
   ```
   DISCORD_TOKEN=ضع_التوكن_هنا_بعد_علامة_اليساوي
   ```
4. **احذف كل ما بعد علامة المساواة** والصق التوكن مكانه، بحيث يصبح السطر هكذا بالضبط:
   ```
   DISCORD_TOKEN=التوكن_الذي_نسخته
   ```
   - ⚠️ لا مسافات قبل التوكن ولا بعده، ولا علامات تنصيص، ولا كلمة `Bot` قبله.
5. **احفظ الملف** (Ctrl+S).

### 1.4 اترك `GUILD_ID` فارغاً

لا تشغل بالك به — السكربت في الخطوة التالية سيضبطه **تلقائياً** من سيرفراتك.
(لو أردت وضعه يدوياً: ديسكورد → ⚙️ Settings → Advanced → فعّل **Developer Mode**
→ يمين على سيرفرك → **Copy Server ID** → الصقه أمام `GUILD_ID=`.)

---

## 🧪 الخطوة 2 — تأكّد واضبط كل شيء تلقائياً (ثانيتان)

من داخل مجلد المشروع، شغّل:

```bash
python setup_discord.py
```

هذا السكربت يتصل بديسكورد ويقوم بكل شيء وحده:
- ✅ يتأكد أن التوكن يعمل ويطبع **اسم بوتك**.
- ✅ يجد سيرفراتك ويكتب `GUILD_ID` تلقائياً (فيظهر أمر `/roles` **فوراً** بدل الانتظار ساعة).
- ✅ يطبع لك **رابط دعوة البوت** الصحيح (لو لم تكن أضفته لسيرفر بعد).
- ✅ لا يعرض التوكن أبداً على الشاشة.

وإن أردت تشخيصاً أوسع للإعدادات: `python check_setup.py`.

> إن ظهر `✅ التوكن يعمل` فأنت جاهز تماماً. وإن ظهر `❌` فالحلّ مكتوب أمامك بالعربي.

---

## 🖥️ الخطوة 3 — شغّل البوت محلياً (اختبار قبل الاستضافة)

**في ويندوز:** اضغط مرتين (Double-click) على الملف **`START.bat`** — يفعل كل شيء وحده.

**في لينكس/ماك:**
```bash
bash start.sh
```

أو يدوياً في أي نظام:
```bash
python -m venv .venv
.venv\Scripts\activate          # ويندوز
source .venv/bin/activate       # لينكس/ماك
pip install -r requirements.txt
python bot.py
```

### ثم: أضف البوت إلى سيرفرك

1. ارجع إلى <https://discord.com/developers/applications> → تطبيقك → **OAuth2 → General**.
2. انسخ **Client ID** (رقم طويل).
3. بدّله في هذا الرابط وافتحه في المتصفح:

```
https://discord.com/api/oauth2/authorize?client_id=ضع_الـ_CLIENT_ID_هنا&permissions=19456&scope=bot%20applications.commands
```

4. اختر سيرفرك → **Authorize**.
   - 💡 الرقم `19456` = عرض الروم + إرسال الرسائل + إدراج روابط (Embed) — وهذا كل ما
     يحتاجه البوت. **لا تعطِ البوت صلاحية Administrator أبداً.**

### الآن جرّبه

في أي روم اكتب:

```
/roles
```

ستظهر لوحة الأزرار. اضغط أي دور → ستأتيك بطاقة الدور **لك وحدك** (لا يراها أحد غيرك) ✅

وإن أردت شرحاً للاستخدام اكتب أيّاً من الاثنين (نفس البطاقة العربية):

```
/help
```

أو

```
/مساعدة
```

> إن أردت أيضاً الأمرين `!roles` و `!help`: اجعل `ENABLE_PREFIX_COMMANDS=true` في `.env`، ثّم فعّل من لوحة
> المطوّرين → Bot → **Message Content Intent** ثم أعد تشغيل البوت.

---

## ☁️ الخطوة 4 — الاستضافة المجانية 24/7 (الطريقة اليدوية)

> ⚡ عندك بديل أسرع: قسم **الطريق السريع** أعلى الصفحة ينفّذ كل هذا بأمر واحد.

الخيار الموصى به **Render** — مجاني، وملف `render.yaml` جاهز في المشروع
(يستخدمه سكربت `deploy.py` أيضاً، وكذلك Blueprint في لوحة Render).

### 4.1 ارفع المشروع على GitHub (مستودع **خاص**)

1. أنشئ المستودع **فارغاً** من <https://github.com/new> — الاسم `ggd-roles-bot` بالضبط،
   والنوع **Private** ⚠️ (ولا تُضف README ولا .gitignore، المستودع يجب أن يبدأ فارغاً).
2. اضغط مرتين على الملف **`push.bat`** في مجلد المشروع (يعمل كل شيء وحده):
   يكتب لك اسم مستخدمك على GitHub → يربط المستودع → يرفع الكود.

أو يدوياً من داخل مجلد المشروع:

```bash
git remote add origin https://github.com/USERNAME/ggd-roles-bot.git
git branch -M main
git push -u origin main
```

> 💡 ملف `.gitignore` يمنع رفع `.env` تلقائياً — أي أن التوكن **لن** يصل إلى GitHub.
> تأكد من ذلك: افتح المستودع وشاهد الملفات، لن تجد ملف `.env`.
> (صندوق `git status` في المشروع نظيف، ولم يُسجّل أي ملف سرّي.)

### 4.2 انشر على Render — بطريقة Blueprint (أسهل طريقة)

المشروع فيه ملف `render.yaml`، وهذا معناه أن Render ستضبط كل شيء (الأمر، المنفذ، فحص الصحة)
تلقائياً — لن تكتب أي إعداد يدوياً:

1. افتح <https://dashboard.render.com> → **New +** → اختر **Blueprint** (وليس Web Service).
2. اربط حساب GitHub → اختر مستودع `ggd-roles-bot`.
3. Render ستقرأ `render.yaml` وتطلب منك قيم المتغيرات السرّية، الصقها كما يلي:

   | المفتاح (Key) | القيمة (Value) |
   |---|---|
   | `DISCORD_TOKEN` | التوكن (نفس اللي في `.env`) |
   | `GUILD_ID` | `1546676344967012383` |
   | `ALLOWED_GUILD_IDS` | `1546676344967012383` |
   | `PUBLIC_URL` | `https://ggd-roles-bot.onrender.com` |
   | `HEALTH_TOKEN` | اتركه فارغاً |

4. اضغط **Apply** / **Create Resources** وانتظر حتى تصبح الحالة **Live** (‏2–4 دقائق).
5. لو الرابط اللي ظهر مختلف عن المكتوب في `PUBLIC_URL` (مثل `ggd-roles-bot-ab12.onrender.com`):
   انسخه → **Environment** → عدّل `PUBLIC_URL` → **Save** (ستُعاد النشرة وحدها).
6. اذهب إلى ديسكورد واكتب `/roles` ✅ — البوت الآن يعمل 24/7 بلا جهازك.

> **نسيت خطوة أو لا تريد Blueprint؟** اختر **New + → Web Service** وربط المستودع يدوياً،
> والضبط حينها: Build `pip install -r requirements.txt` — Start `python -u bot.py` — Health `/healthz`.
> ولا تنسَ كل متغيرات الجدول أعلاه من **Environment**.

> **لماذا `PUBLIC_URL` مهم؟** خطة Render المجانية تُنيّم الخدمة بعد **15 دقيقة** بلا زيارات.
> الكود يزور نفسه كل **10 دقائق** تلقائياً فيبقى مستيقظاً — وهذا ما يجعل 24/7 ممكناً مجاناً.

**نقاط مهمّة:** استخدم خدمة ويب واحدة فقط (الحصة المجانية 750 ساعة شهرياً = خدمة واحدة طوال الشهر)،
ولا تجعل `SELF_PING_SECONDS` أكبر من 900.

---

## 🧯 لو حدثت مشكلة

| ما تراه | الحل |
|---|---|
| `❌ لم أجد التوكن` | لم تحفظ `.env` بعد اللصق، أو حفظته في مكان آخر |
| `❌ GitHub رفض المفتاح (401)` | مفتاح GitHub ناقص/منتهٍ — اعمل واحداً جديداً والصقه كاملاً |
| `❌ Render رفض المفتاح (401)` | مفتاح Render ناقص — اعمل واحداً جديداً والصقه كاملاً |
| `❌ ينقصني: GITHUB_TOKEN` | لم تنسخ `.deploy_secrets.example` باسم `.deploy_secrets` |
| `❌ قيمة DISCORD_TOKEN لا تبدو توكن صحيح` | نسخت جزءاً من التوكن أو الصقت النصّ التجريبي — أعد Copy |
| `401` عند الفحص | التوكن أُلغي أو خاطئ — اضغط Reset Token وخذ جديداً |
| `/roles` لا يظهر | ضع `GUILD_ID` وأعد التشغيل (الأمر العالمي يتأخّر حتى ساعة) |
| الأزرار لا تستجيب | البوت متوقف — أعد إرسال اللوحة بعد تشغيله |
| الصور لا تظهر | نتيجة حجب مؤقت من موقع الصور؛ باقي البطاقة يعمل عادي |
| السيرفر ينام | تأكد من `PUBLIC_URL` الصحيح، أو استخدم UptimeRobot المجاني كل 5 دقائق |

**قواعد أمنية لا تتجاوزها:**
0. لتقصر البوت على سيرفراتك وحدها (يخرج تلقائياً من أي سيرفر غريب):
   `python setup_discord.py --lock`
1. لا تشارك التوكن مع أي شخص (ولو كان «بوت آخر» أو «موقع فحص»).
2. لا تعطِ البوت صلاحية **Administrator** — لا يحتاجها.
3. إن شككت في تسرّب التوكن: **Reset Token** فوراً ثم حدّثه في `.env` وفي Render.

---

## 🧩 بعد كده: أضف أدواراً بنفسك

افتح `bot.py` → قسم `ROLES` → انسخ قالب دور واحد وغيّر النصوص والصورة.
الأزرار والصفحات والألوان تُبنى تلقائياً. الشرح التفصيلي في `README.md`.

وهناك حقلان اختياريان مفيدان: `"maps"` لذكر الخرائط التي يتوفّر فيها الدور،
و `"mode"` للأدوار التي لا تظهر إلا في أوضاع اللعبة الخاصة.

خطوتان لا تنساهما بعد أي إضافة:
1. `python -m unittest test_bot.py` — أضف مفتاح الدور الجديد إلى قائمة `EXPECTED`
   حتى يظل اختبار اكتمال الدليل صادقاً (وهو يفشل لو نُسي دور موجود أصلاً).
2. `python check_images.py` — يتأكد أن رابط الصورة الجديد يعمل فعلاً (كثير من روابط
   ويكي اللعبة لا يمكن تخمينها، فالصورة المكسورة لا تظهر إلا في ديسكورد).
